language: rust
rust:
  - stable
  - beta
  - nightly
script:
  - sudo bash ./ci_build.sh
matrix:
  include:
    - os: linux
      sudo: required
      dist: trusty
      addons:
        apt:
          sources:
            - llvm-toolchain-trusty-5.0
          packages:
            - clang-5.0
